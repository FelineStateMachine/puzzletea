# Takuzu+

Takuzu+ keeps the classic Takuzu rules and adds fixed relation clues between adjacent cells.

- `=` means the two cells must be equal.
- `x` means the two cells must be opposite.

The controls match classic Takuzu.

